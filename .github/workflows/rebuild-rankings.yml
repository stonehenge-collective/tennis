name: "🏆 Rebuild Rankings"

on:
  push:
    branches: [main]
    paths: ["singles-matches/**", "doubles-matches/**"]
  workflow_dispatch:
    inputs:
      logLevel:
        description: "Log level"
        required: true
        default: "warning"
        type: choice
        options:
          - info
          - warning
          - debug
      print_tags:
        description: "True to print to STDOUT"
        required: true
        type: boolean
      tags:
        description: "Test scenario tags"
        required: true
        type: string
      environment:
        description: "Environment to run tests against"
        type: environment
        required: true

jobs:
  rebuild-rankings-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Setup Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync

      - name: Generate ranking data
        run: |
          mkdir -p temp-rankings
          uv run python -m scripts.generate_singles_ranking > temp-rankings/singles-ranking.csv
          uv run python -m scripts.generate_doubles_ranking
          mv doubles-ranking.csv temp-rankings/doubles-ranking.csv
          mv doubles-individual-ranking.csv temp-rankings/doubles-individual-ranking.csv

      - name: Build static site (leaderboard and history)
        id: build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: uv run python -m scripts.build_pages

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ steps.build.outputs.temp_dir }}

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
